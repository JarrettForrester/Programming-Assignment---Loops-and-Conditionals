# 4.1
# Assign a random number between 1 and 10 to the variable secret
    secret = 7
   
# Assign another random number between 1 and 10 to the variable guess
    guess = 4

# Write conditional tests to print 'too low' if guess is less than secret, 'too high' if greater than secret, and 'just right' if equal to secret
    if guess < secret:
        print('too low')
    elif guess > secret:
        print('too high')
    else:
        print('just right')
